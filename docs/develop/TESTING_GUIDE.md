# 📋 测试指南

## 🚀 快速开始测试（5分钟）

### 准备工作

#### 步骤1：创建配置文件

在项目根目录执行：

```bash
cd /Users/wangshiwen/Desktop/workspace/diary_app

# 创建必需的目录
mkdir -p assets/configs assets/prompts

# 创建 styles.json
cat > assets/configs/styles.json << 'EOF'
{
  "styles": [
    {
      "id": "warm",
      "name": "温馨",
      "icon": "❤️",
      "description": "温柔细腻，充满爱意",
      "word_count_min": 300,
      "word_count_max": 500,
      "emoji_count": 3
    },
    {
      "id": "poetic",
      "name": "诗意",
      "icon": "🌙",
      "description": "文艺浪漫，富有诗意",
      "word_count_min": 400,
      "word_count_max": 600,
      "emoji_count": 2
    },
    {
      "id": "real",
      "name": "真实",
      "icon": "😊",
      "description": "口语化，原汁原味",
      "word_count_min": 200,
      "word_count_max": 400,
      "emoji_count": 4
    }
  ],
  "default_style": "warm"
}
EOF

# 创建 defaults.json
cat > assets/configs/defaults.json << 'EOF'
{
  "default_style": "warm",
  "default_word_count": 400,
  "mood_tags": [
    {"id": "happy", "name": "开心", "emoji": "😊", "color": "#FFB74D"},
    {"id": "sweet", "name": "甜蜜", "emoji": "💕", "color": "#F06292"},
    {"id": "miss", "name": "想念", "emoji": "🥺", "color": "#64B5F6"},
    {"id": "excited", "name": "激动", "emoji": "🤩", "color": "#BA68C8"},
    {"id": "calm", "name": "平静", "emoji": "😌", "color": "#4DB6AC"}
  ],
  "input_placeholders": [
    "今天他给我买了奶茶，还记得少糖...",
    "一起看了日落，他说下次还要带我去海边...",
    "异地第37天，好想抱抱他...",
    "他今天发了很多表情包，笑死我了..."
  ]
}
EOF

# 创建 base_diary.txt
cat > assets/prompts/base_diary.txt << 'EOF'
你是一个温柔细腻的情感记录者，擅长将简短的记录扩写成感人的日记。

请基于以下信息生成一篇日记：
- 内容: {{content}}
- 风格: {{style}}
- 心情: {{mood}}

要求：
1. 保持原汁原味的情感
2. 适当扩充细节
3. 字数控制在300-500字
4. 使用Markdown格式
5. 适当添加emoji表情
EOF

echo "✅ 配置文件创建完成！"
```

#### 步骤2：生成Hive适配器代码

```bash
flutter packages pub run build_runner build --delete-conflicting-outputs
```

预期输出：
```
[INFO] Generating build script completed, took 342ms
[INFO] Creating build script snapshot... completed, took 8.2s
[INFO] Building new asset graph completed, took 543ms
[INFO] Checking for unexpected pre-existing outputs. completed, took 1ms
[INFO] Running build completed, took 3.1s
[INFO] Caching finalized dependency graph completed, took 32ms
[INFO] Succeeded after 3.2s with 2 outputs
```

#### 步骤3：检查可用设备

```bash
flutter devices
```

你应该能看到类似输出：
```
3 connected devices:

iPhone 15 Pro (mobile) • xxx • ios • com.apple.CoreSimulator.SimRuntime.iOS-17-0 (simulator)
macOS (desktop)        • macos • darwin-arm64 • macOS 14.0
Chrome (web)           • chrome • web-javascript • Google Chrome 119.0
```

#### 步骤4：运行应用

```bash
# 推荐：在Chrome浏览器中运行（开发调试最方便）
flutter run -d chrome

# 或在iOS模拟器运行
flutter run -d "iPhone 15 Pro"

# 或让Flutter自动选择设备
flutter run
```

---

## 🧪 功能测试清单

### 1. 首页测试 (HomePage)

#### 测试场景A：空状态
**步骤**：
1. 首次启动应用
2. 观察首页显示

**预期结果**：
- ✅ 显示空状态提示："还没有日记哦"
- ✅ 显示书本图标
- ✅ 显示提示文字："点击下方按钮，记录你们的第一天"
- ✅ 底部显示"记录今天"按钮

#### 测试场景B：日记列表（需要先创建数据）
**步骤**：
1. 在main.dart中启用Mock数据初始化（见下文）
2. 重启应用
3. 观察首页列表

**预期结果**：
- ✅ 显示日记卡片列表
- ✅ 每个卡片显示日期、心情图标、内容预览
- ✅ 有AI生成标记的显示"AI已生成"
- ✅ 卡片有光晕效果
- ✅ 列表可滚动

**如何启用Mock数据**：

在 `lib/main.dart` 的 `main()` 函数中添加：

```dart
void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  
  // 初始化Hive
  await Hive.initFlutter();
  Hive.registerAdapter(DiaryAdapter());
  
  // 🔥 添加这段代码生成Mock数据
  final repo = LocalDiaryRepository();
  await repo.initMockData(count: 10);
  
  runApp(const ProviderScope(child: MyApp()));
}
```

#### 测试场景C：导航功能
**步骤**：
1. 点击任意日记卡片
2. 点击右上角"设置"按钮
3. 点击底部"记录今天"按钮

**预期结果**：
- ✅ 点击卡片跳转到详情页
- ✅ 点击设置跳转到设置页
- ✅ 点击"记录今天"跳转到创建页

---

### 2. 创建日记测试 (CreateDiaryPage)

#### 测试场景A：心情选择
**步骤**：
1. 点击"记录今天"按钮
2. 观察心情选择区域
3. 依次点击不同心情

**预期结果**：
- ✅ 显示5个心情选项（开心、甜蜜、想念、激动、平静）
- ✅ 每个选项有emoji和文字
- ✅ 点击后有选中效果（边框加粗、背景色变化）
- ✅ 可以切换不同心情
- ✅ 可以取消选择（再次点击）

#### 测试场景B：风格选择
**步骤**：
1. 观察风格选择区域
2. 依次点击不同风格

**预期结果**：
- ✅ 显示3个风格选项（温馨、诗意、真实）
- ✅ 每个选项显示图标、名称、描述
- ✅ 默认选中"温馨"
- ✅ 点击后切换选中状态
- ✅ 只能单选

#### 测试场景C：内容输入
**步骤**：
1. 点击输入框
2. 输入文字："今天和他一起看了电影"

**预期结果**：
- ✅ 输入框获得焦点
- ✅ 键盘弹出
- ✅ 可以正常输入中英文
- ✅ 可以多行输入
- ✅ 有占位符提示

#### 测试场景D：AI生成（Mock）
**步骤**：
1. 选择心情："甜蜜"
2. 选择风格："温馨"
3. 输入内容："今天和他一起看了电影"
4. 点击右上角"AI生成"按钮
5. 等待2秒

**预期结果**：
- ✅ 按钮显示"生成中..."
- ✅ 显示加载动画
- ✅ 2秒后显示"保存成功"提示
- ✅ 自动返回首页
- ✅ 首页列表顶部出现新创建的日记

#### 测试场景E：空内容验证
**步骤**：
1. 不输入任何内容
2. 直接点击"AI生成"

**预期结果**：
- ✅ 显示提示："请先输入内容"
- ✅ 不执行生成操作

---

### 3. 日记详情测试 (DiaryDetailPage)

#### 测试场景A：内容显示
**步骤**：
1. 从首页点击任意日记
2. 观察详情页内容

**预期结果**：
- ✅ 顶部显示完整日期（年月日 星期）
- ✅ 显示心情卡片（如果有）
- ✅ 显示"我的记录"区域（原始内容）
- ✅ 显示"AI美化版"区域（如果有AI内容）
- ✅ AI内容有渐变背景
- ✅ 内容可滚动

#### 测试场景B：编辑功能
**步骤**：
1. 在详情页点击右上角"编辑"按钮
2. 修改原始内容："今天和他一起看了电影，很开心"
3. 点击"保存"

**预期结果**：
- ✅ 跳转到编辑页面
- ✅ 显示当前内容可编辑
- ✅ 保存后显示"保存成功"
- ✅ 返回详情页
- ✅ 内容已更新

#### 测试场景C：分享功能
**步骤**：
1. 点击右上角"分享"按钮

**预期结果**：
- ✅ 弹出系统分享面板
- ✅ 分享内容包含日期、原始内容、AI内容
- ✅ 可以选择分享途径（微信、短信等）

#### 测试场景D：复制功能
**步骤**：
1. 点击右上角"..."菜单
2. 选择"复制原始内容"
3. 粘贴到其他地方验证

**预期结果**：
- ✅ 显示菜单
- ✅ 显示"已复制到剪贴板"提示
- ✅ 剪贴板有正确内容

---

### 4. 编辑日记测试 (EditDiaryPage)

#### 测试场景A：原始内容编辑
**步骤**：
1. 进入编辑页面
2. 修改"原始记录"区域的文字
3. 点击"保存"

**预期结果**：
- ✅ 可以编辑文字
- ✅ 支持多行编辑
- ✅ 保存成功
- ✅ 返回详情页内容已更新

#### 测试场景B：AI内容编辑
**步骤**：
1. 修改"AI美化版"区域
2. 测试Markdown格式（加粗、斜体等）
3. 点击"保存"

**预期结果**：
- ✅ 显示Markdown编辑工具栏
- ✅ 可以插入Markdown标记
- ✅ 保存成功
- ✅ 详情页正确显示Markdown效果

#### 测试场景C：重新生成按钮
**步骤**：
1. 点击"重新生成"按钮

**预期结果**：
- ✅ 按钮存在（目前功能待实现）

---

### 5. 设置页面测试 (SettingsPage)

#### 测试场景A：页面显示
**步骤**：
1. 从首页点击右上角"设置"
2. 观察页面内容

**预期结果**：
- ✅ 显示各个设置分组
- ✅ 情侣信息、AI设置、数据管理、外观、其他
- ✅ 每个选项有图标和说明
- ✅ 列表可滚动

#### 测试场景B：关于对话框
**步骤**：
1. 点击"关于我们"
2. 观察对话框

**预期结果**：
- ✅ 显示应用名称
- ✅ 显示版本号：1.0.0
- ✅ 显示应用图标
- ✅ 显示应用描述

---

### 6. 删除功能测试

#### 测试场景A：删除确认
**步骤**：
1. 在首页点击日记卡片的删除按钮
2. 观察确认对话框
3. 点击"取消"

**预期结果**：
- ✅ 显示确认对话框
- ✅ 提示"确定要删除这篇日记吗？"
- ✅ 有"取消"和"删除"按钮
- ✅ 点击取消关闭对话框，日记仍存在

#### 测试场景B：确认删除
**步骤**：
1. 再次点击删除按钮
2. 点击"删除"确认

**预期结果**：
- ✅ 显示"已删除"提示
- ✅ 日记从列表消失
- ✅ 列表自动更新

---

## 🎨 UI/UX 测试

### 主题测试

#### 浅色主题
**步骤**：
1. 系统设置为浅色模式
2. 启动应用

**检查项**：
- ✅ 背景是浅色
- ✅ 文字清晰可读
- ✅ 卡片有阴影效果
- ✅ 颜色对比度合适

#### 深色主题
**步骤**：
1. 系统设置为深色模式
2. 重启应用

**检查项**：
- ✅ 背景是深色
- ✅ 文字清晰可读（浅色）
- ✅ 卡片效果适配深色
- ✅ 不刺眼

### 响应式测试

#### 不同屏幕尺寸
**测试设备**：
- iPhone SE (小屏)
- iPhone 15 Pro (中屏)
- iPad (大屏)

**检查项**：
- ✅ 布局不错乱
- ✅ 文字大小合适
- ✅ 按钮可点击区域足够大
- ✅ 横屏显示正常（可选）

### 性能测试

#### 列表滚动
**步骤**：
1. 生成50条Mock数据
2. 快速滚动列表

**检查项**：
- ✅ 滚动流畅，无卡顿
- ✅ 帧率保持60fps
- ✅ 没有白屏闪烁

#### 页面切换
**步骤**：
1. 快速在各个页面间切换

**检查项**：
- ✅ 切换动画流畅
- ✅ 无内存泄漏
- ✅ 返回速度快

---

## 🐛 边界情况测试

### 数据测试

#### 空数据
- ✅ 空内容提示正确
- ✅ 无日记时首页显示空状态

#### 超长内容
- ✅ 输入超长文本不崩溃
- ✅ 显示时正确截断
- ✅ 详情页完整显示

#### 特殊字符
- ✅ Emoji正确显示
- ✅ 换行符正确处理
- ✅ 特殊符号不影响功能

---

## 📊 测试报告模板

完成测试后，填写此表格：

| 测试项 | 状态 | 问题描述 | 截图 |
|-------|------|---------|------|
| 首页空状态 | ✅/❌ | | |
| 首页列表显示 | ✅/❌ | | |
| 创建日记-心情选择 | ✅/❌ | | |
| 创建日记-风格选择 | ✅/❌ | | |
| 创建日记-AI生成 | ✅/❌ | | |
| 详情页显示 | ✅/❌ | | |
| 编辑功能 | ✅/❌ | | |
| 删除功能 | ✅/❌ | | |
| 分享功能 | ✅/❌ | | |
| 主题适配 | ✅/❌ | | |
| 性能表现 | ✅/❌ | | |

---

## 🔧 调试工具

### Flutter DevTools

启动DevTools：
```bash
flutter pub global activate devtools
flutter pub global run devtools
```

在应用运行时打开DevTools可以：
- 查看Widget树
- 监控性能
- 查看网络请求
- 检查内存使用

### 日志输出

在代码中添加调试日志：
```dart
print('调试信息: $变量');
debugPrint('调试: 更详细的信息');
```

查看日志：
```bash
flutter logs
```

### 热重载快捷键

- `r` - 热重载（Hot Reload）
- `R` - 热重启（Hot Restart）
- `p` - 显示性能叠加层
- `w` - 显示Widget层级
- `q` - 退出应用

---

## ❓ 常见问题

### Q1: 配置文件找不到
**现象**: 运行时报错，提示找不到assets文件

**解决**:
1. 检查pubspec.yaml中assets配置
2. 运行 `flutter clean && flutter pub get`
3. 重新运行应用

### Q2: Hive报错
**现象**: Unable to find generated Adapter

**解决**:
```bash
flutter packages pub run build_runner build --delete-conflicting-outputs
```

### Q3: Mock数据不显示
**现象**: 首页是空的

**解决**:
在main.dart中添加Mock数据初始化代码（见上文）

### Q4: 热重载不生效
**现象**: 修改代码后按r没反应

**解决**:
- 对于状态变化，需要按R热重启
- 对于某些文件（main.dart），需要完全重启

---

## 📝 测试记录

**测试日期**: ___________  
**测试人员**: ___________  
**应用版本**: 1.0.0  
**Flutter版本**: ___________  
**测试设备**: ___________  

**总体评价**: ⭐⭐⭐⭐⭐

**发现问题数量**: 
- 🔴 严重: ___
- 🟡 一般: ___
- 🔵 轻微: ___

**备注**: 
___________________________________________
___________________________________________

---

祝测试顺利！🎉
